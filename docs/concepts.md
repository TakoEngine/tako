# Concepts

## Composable systems

While many designs of this engine is influenced by ECS, you are not forced to use it,
as many modules are designed to be used standalone (to a certain degree).

## Resource Handles

Most resources like textures are returned as handles instead of pointers.
